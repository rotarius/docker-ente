# PostgreSQL
POSTGRES_USER=pguser
POSTGRES_DB=ente_db
POSTGRES_PASSWORD=changeme

# POSTGRES_PASSWORD must also be entered into museum.yaml (db.password) - see
# museum.yaml.example.
#
# Object storage (MinIO) is provided by the shared docker-minio instance, not
# by this repo. Get the S3 key/secret to put into museum.yaml from there.
